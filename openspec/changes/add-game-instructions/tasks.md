## 1. Instructions Component

- [x] 1.1 Create `Instructions.js` component in `src/frontend/src/components/` with collapsible panel, header toggle, and instructions content
- [x] 1.2 Create `Instructions.css` with styles for the panel header, body, toggle indicator, and collapsed/expanded states
- [x] 1.3 Use `localStorage` key `puzzleInstructionsCollapsed` to persist and restore the collapsed/expanded state

## 2. Integration

- [x] 2.1 Import and render `Instructions` component in `App.js` between the header and the workspace card
- [x] 2.2 Adjust `App.css` if needed to ensure proper spacing between instructions panel and workspace card

## 3. Verification

- [x] 3.1 Verify instructions panel is expanded on first visit and displays puzzle goal and all controls
- [x] 3.2 Verify clicking the header toggles between expanded and collapsed states
- [x] 3.3 Verify collapsed state persists after page reload
- [x] 3.4 Build frontend to confirm no compilation errors
